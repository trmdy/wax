# Differential adjudications

This ledger records disagreements where inspection shows that wax is correct
and the SheetJS oracle is wrong. A raw disagreement is never resolved in
SheetJS's favour automatically: each verdict requires file-specific evidence
and reasoning. Keep paths corpus-relative where possible and do not include
private file contents.

| File | Sheet / cell | Field | Verdict | Reasoning and evidence | Date |
| --- | --- | --- | --- | --- | --- |
| `corpus/files/sheetjs/test_files/time_stress_test_1.xlsb` | `Sheet1!B10001:J12501` (representative: `B10001`, `H10001:J10001`) | `v` for `t:"d"` | wax correct; SheetJS loses fractional seconds | The cached serial at row 10001 resolves to `1899-12-31T02:07:53.184`. wax preserves `.184`; SheetJS emits `1899-12-31T02:07:53` while its own displays for the same row are `53.2`, `53.18`, and `53.184` under `ss.0`, `ss.00`, and `ss.000`. The oracle's ISO value therefore contradicts both the stored serial and its own millisecond display. This systematic truncation accounts for 108,801 XLSB date/date mismatches in the full 2,048-file run. | 2026-07-28 |
| `corpus/files/sheetjs/test_files/time_stress_test_1.xlsb` | `Sheet1!K2:P12501` | `t` / `v` for elapsed-time formats | wax correct; SheetJS converts durations to pseudo-dates | The six columns use `[h]`, `[hh]`, `[m]`, `[mm]`, `[s]`, and `[ss]`, which are elapsed-time/duration formats. For cached serial `0.9`, wax retains numeric `0.9` and renders elapsed values such as `22`, `1296`, and `77760`; SheetJS changes the value to calendar date `1899-12-31T21:36:00`. The normalized model has a date type but no duration type, so retaining the serial as `t:"n"` is the non-lossy contract representation. This accounts for 74,994 XLSB number/date mismatches in the full run. | 2026-07-28 |
| `corpus/files/{openpyxl/3.1.5/openpyxl/reader/tests/data/null_file.xlsx,sheetjs/test_files/openpyxl_r_null_file.xlsx}` | whole file | open result | wax correct; inputs are empty, not workbooks | Both payloads are zero bytes (`file` reports `empty`) and have no ZIP end-of-central-directory record. wax returns structured `bad_zip`; SheetJS reports successful empty workbooks. Accepting an empty byte stream as XLSX would hide corruption. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/{54764.xlsx,54764-2.xlsx,poc-xmlbomb.xlsx,poc-xmlbomb-empty.xlsx}` | XML package parts | open result | wax correct; entity-expansion payloads are rejected | The ZIP containers are intact, but their XML parts reference nested custom entities (`&lol9;` or `&a5;`). wax rejects the undeclared/unsafe entities as `bad_zip`; SheetJS reports success. These are Apache POI XML-bomb security fixtures, so expansion or lenient acceptance is not a compatibility win. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/poc-shared-strings.xlsx` | shared strings / dimensions | open result | wax correct; amplification fixture is rejected | wax stops with a structured parse failure (`Range is missing the expected row component`). The SheetJS oracle process reaches the 4 GiB V8 heap limit and aborts with `SIGABRT`. Rejecting this malformed amplification fixture is the safe and correct result. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/xlsx-corrupted.xlsx,sheetjs/test_files/roo_Pfand_from_windows_phone.xlsx}` | relationship parts | open result | wax correct; relationship XML is truncated | Both ZIP containers pass `unzip -t`, but the relationship part ends before the required `</Relationships>` close tag. wax returns `bad_zip` with the exact unexpected-EOF evidence; SheetJS opens them leniently. The package XML is structurally corrupt. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/51535.xls,sheetjs/test_files/apachepoi_51535.xls}` | whole file | open result | wax correct; declared-extent bomb rejected | The 41,984-byte file declares a full 65,536x256 BIFF extent (16,777,216 cells, zero real cells); materializing it cost wax ~1.03 GiB transient RSS (measured 1,077,821,440 B peak) and was the corpus RSS outlier. The W3C preflight now rejects it as `bomb` in 2 ms at 2.4 MiB peak RSS. SheetJS "opens" it to an empty sheet; a structured bomb rejection is the correct contract behavior for a hostile declared extent. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/{clusterfuzz-testcase-minimized-POIXSSFFuzzer-5265527465181184.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-5937385319563264.xlsx}` | XML package parts | open result | wax correct; fuzzer-crafted corruption rejected | These are Apache POI ClusterFuzz minimized testcases — files crafted by a fuzzer to break parsers. Their zip entries fail checksum validation (`invalid XML: I/O error: Invalid checksum`). wax rejects them structurally as `bad_zip`; lenient acceptance of checksum-failing archive entries is not a compatibility win. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/60825.xlsx` | workbook sheet `Module` | open result | wax correct; the package has an unresolvable sheet relationship | `xl/workbook.xml` stores `<sheet name="Module" ... r:id=""/>`; `xl/_rels/workbook.xml.rels` contains only `rId1` through `rId3` and no empty relationship ID or target for that sheet. wax returns structured `bad_zip` (`Relationship not found`); SheetJS silently invents an empty sheet. The stored package cannot resolve the declared sheet, so rejection is the non-lossy result. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/{61045_govdocs1_626534.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-5436547081830400.xls}` | BIFF / CFB structure | open result | wax correct; structurally invalid legacy records rejected | `61045` contains a 16-byte BIFF `DIMENSIONS` record where the selected BIFF version requires the other record width; wax reports that exact invalid length. The ClusterFuzz file has a CFB chain pointing to sector `4294967274`, past the container. SheetJS opens both leniently, but neither malformed address can be followed without fabricating structure. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/{clusterfuzz-testcase-minimized-POIFuzzer-5040805309710336.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-4828727001088000.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-5089447305609216.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-5185049589579776.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-6123461607817216.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-6419366255919104.xlsx,clusterfuzz-testcase-minimized-POIXSSFFuzzer-6448258963341312.xlsx,clusterfuzz-testcase-minimized-XLSX2CSVFuzzer-5025401116950528.xlsx,clusterfuzz-testcase-minimized-XLSX2CSVFuzzer-5542865479270400.xlsx,clusterfuzz-testcase-minimized-XLSX2CSVFuzzer-5636439151607808.xlsx,clusterfuzz-testcase-minimized-XLSX2CSVFuzzer-6504225896792064.xlsx,clusterfuzz-testcase-minimized-XLSX2CSVFuzzer-6594557414080512.xlsx,crash-274d6342e4842d61be0fb48eaadad6208ae767ae.xlsx,crash-9bf3cd4bd6f50a8a9339d363c2c7af14b536865c.xlsx}` | ZIP central directory | open result | wax correct; minimized ZIP corruptions rejected | Every named payload lacks a valid EOCD or stores a CDFH offset outside the archive. The SheetJS subprocess also rejects every file (`unreadable`), commonly reporting an impossible compressed size or compression method. wax's structured `bad_zip` preserves the same corruption boundary without attempting partial archive recovery. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/{clusterfuzz-testcase-minimized-POIHSSFFuzzer-4651309315719168.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-4657005060816896.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-4734163573080064.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-4819588401201152.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-5175219985448960.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-5786329142919168.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-5816431116615680.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-5889658057523200.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-6137883240824832.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-6322470200934400.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-6483562584932352.xls,clusterfuzz-testcase-minimized-POIHSSFFuzzer-6537773940867072.xls}` | CFB FAT, DIFAT, directory, and mini-FAT | open result | wax correct; minimized compound-container corruptions rejected | Per-file preflight errors identify impossible structures: FAT tables with more entries than file sectors, a DIFAT sector beyond EOF, object type `18`, `29,440` directory sectors in a 27-sector file, or `808,150,272` mini-FAT sectors in a 3-sector file. SheetJS rejects all twelve as unreadable. These are concrete container corruptions, not reader coverage gaps. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/58616.xlsx,poi/test-data/spreadsheet/61300.xls,poi/test-data/spreadsheet/deep-data.xlsx,poi/test-data/spreadsheet/protected_passtika.xlsb,poi/test-data/spreadsheet/protected_passtika.xlsx,poi/test-data/spreadsheet/rde.imf.ru_sites_default_files_rde_documents_vodootvedenie_2020.xlsb.xls,sheetjs/test_files/apachepoi_password.xls.xlsb,sheetjs/test_files/excel-reader-xlsx_error03.xlsx,sheetjs/test_files/excel-reader-xlsx_error04.xlsx,sheetjs/test_files/excel-reader-xlsx_error05.xlsx,sheetjs/test_files/openpyxl_r_null_archive.xlsx,sheetjs/test_files/roo_encrypted-letmein.ods}` | whole file / package structure | open result | wax correct; corrupt, encrypted, or non-workbook payloads rejected | The named set has file-specific structural evidence: absent EOCD, password-protected OLE payloads under ZIP suffixes, a 4,076,863,688-byte stream declared inside a 61,952-byte CFB, truncated `</workbook>` XML, no `_rels/.rels`, malformed encrypted ODS XML, or (for `xlsx_error05`) plain ASCII text beginning `This is just a t`. SheetJS also rejects every file except that plain-text case, which it treats as an empty workbook. wax reports structured `bad_zip` instead of fabricating data. | 2026-07-28 |
| `corpus/files/sheetjs/test_files/pyExcelerator_P-0508-0000507647-3280-5298.xls` | CFB FAT | open result | wax correct; impossible FAT declaration rejected | `file(1)` cannot read the sector allocation table, and wax measures 109 declared FAT sectors although only 9 sectors fit in the container. SheetJS opens leniently, but following that allocation table requires reading outside the stored file. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/35897-type4.xls,poi/test-data/spreadsheet/50833.xls,poi/test-data/spreadsheet/51832.xls,poi/test-data/spreadsheet/60284.xls,poi/test-data/spreadsheet/password.xls,poi/test-data/spreadsheet/xor-encryption-abc.xls,sheetjs/test_files/apachepoi_50833.xls,sheetjs/test_files/apachepoi_51832.xls,sheetjs/test_files/apachepoi_password.xls,sheetjs/test_files/apachepoi_xor-encryption-abc.xls}` | BIFF declared dimensions | open result | wax correct; hostile or encrypted extents rejected | These files declare between 56,854,512 and 103,924,570,068,677 cells, well over the 8,000,000-cell safety rail; the paired oracle errors identify encryption or password protection. SheetJS fails all ten. A bounded `bomb` result is correct and prevents encrypted bytes or corrupt dimensions from driving allocation. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/at.gv.land-oberoesterreich.www_cps_rde_xbcr_SID-4A1B954F-5C07F98E_ooe_stat_download_bp10.xls,sheetjs/test_files/number_format.xls,sheetjs/test_files/number_format.xlsb,sheetjs/test_files/number_format.xlsm,sheetjs/test_files/LONumbers.xls,sheetjs/test_files/LONumbers-2010.xls,sheetjs/test_files/LONumbers-2011.xls,sheetjs/test_files/LONumbers.xlsx,sheetjs/test_files/LONumbers-2010.xlsx,sheetjs/test_files/LONumbers-2011.xlsx}` | date-valued cells (representative: gov workbook `Sheet1!V5`, `number_format.xls` sheet 1 row 12) | `v` for `t:"d"` | wax correct; SheetJS drops stored milliseconds | The gov cell's cached serial resolves to `2010-04-25T17:42:32.083`; wax emits `.083`, while SheetJS emits `2010-04-25T17:42:32` although both render the same second-level display. `number_format.xls` likewise resolves to `1933-10-18T16:17:36.960`, while SheetJS drops `.960`. Together with the existing `time_stress_test_1.xlsb` adjudication, this is the same systematic cause across the full 112,484 date/date mismatches. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/57181.xlsm,sheetjs/test_files/time_stress_test_1.xlsb,sheetjs/test_files/number_format.ods,sheetjs/test_files/number_format.xls,sheetjs/test_files/number_format.xlsb,sheetjs/test_files/number_format.xlsm,sheetjs/test_files/LONumbers.xls,sheetjs/test_files/LONumbers-2010.xls,sheetjs/test_files/LONumbers-2011.xls,sheetjs/test_files/LONumbers.xlsx,sheetjs/test_files/LONumbers-2010.xlsx,sheetjs/test_files/LONumbers-2011.xlsx}` | duration-formatted cells (representative: `57181.xlsm` sheet 1 `E1`) | `t` / `v` | wax correct; SheetJS converts durations to pseudo-dates | `57181.xlsm!E1` stores `0.020833333333333332` with format `[=0]?;[<4.16666666666667][hh]:mm:ss;[hh]:mm`, yielding `00:30:00`. wax retains the duration serial as numeric; SheetJS emits the calendar fiction `1899-12-31T00:30:00`. The same `[h]`/elapsed semantics cover all 78,005 number/date mismatches, including the previously adjudicated 74,994-cell XLSB block. | 2026-07-28 |
| `corpus/files/sheetjs/test_files/time_stress_test_1.xlsb` | `Sheet1`, representative row 4 formats `[h]`/`[hh]`; analogous `[m]`, `[mm]`, `h`, `hh`, `hm`, `hhmm` columns | `d` | wax correct; SheetJS truncates instead of rounding to the displayed unit | Row 4 stores serial `0.2`, exactly 4.8 hours. With no smaller displayed unit, `[h]` and `[hh]` round to `5` and `05`; wax does so, while SheetJS emits `4` and `04`. The same least-significant-unit rule explains the 5,682–6,106 mismatches in each of the eight hour/minute codes; second-bearing `[s]` and `[ss]` already match 100%, corroborating that the discrepancy is SheetJS's truncation boundary. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/15228.xls,sheetjs/test_files/apachepoi_15228.xls,sheetjs/test_files/apachepoi_15228.xls.xlsb,poi/test-data/spreadsheet/53446.xls,sheetjs/test_files/apachepoi_53446.xls,sheetjs/test_files/apachepoi_53446.xls.xlsb}` | empty-string formula results under accounting formats (representative: `15228.xls`, sheet 7, `B45`) | `d` | wax correct; SheetJS drops format-mandated spacing | `B45` has cached text `""` and format `_(* #,##0_);_(* \(#,##0\);_(* "-"??_);_(@_)`. The selected text section `_(@_)` contains two underscore-width placeholders, so wax emits two spaces; SheetJS emits an empty string. This accounts for 2,976 integer-accounting and 723 currency-accounting display mismatches. | 2026-07-28 |
| `corpus/files/{sheetjs/test_files/fraction-test.xls,poi/test-data/spreadsheet/54686_fraction_formats.xls,sheetjs/test_files/apachepoi_54686_fraction_formats.xls,sheetjs/test_files/apachepoi_54686_fraction_formats.xls.xlsb}` | fraction-formatted numeric cells (representative: `fraction-test.xls!B1` and `H1`) | `d` | wax correct; SheetJS's bounded-denominator approximation is not nearest | Under `# ?/?`, the denominator is limited to one digit. For `0.1`, wax chooses `1/9`, the nearest allowed nonzero fraction, while SheetJS emits `0`; for `0.7`, wax chooses `5/7` (error about 0.0143) while SheetJS chooses `2/3` (error about 0.0333). The same exact bounded search governs the 3,274 mismatches across `# ?/?`, `#\ ??/?????????`, `#\ ?/2`, `# ??/??`, and `#\ ?/4`. | 2026-07-28 |
| `corpus/files/poi/test-data/spreadsheet/bug60858.xlsx` | sheet 1 `F2`, `G2`, `I2`, `L2` | formula presence / cached value | wax correct; SheetJS drops formulas that have no cached `<v>` | Each stored `<c>` contains an `<f>` formula but no `<v>` element. The frozen model permits `f` with a null cached `v`; wax preserves all four formulas and emits null caches. SheetJS omits the four cells entirely, losing authored formula text. | 2026-07-28 |
| W5B final-count correction for the two date rows immediately above | full corpus | `t` / `v` | wax correct; negative serials are numeric, nonnegative date serials preserve milliseconds | The final negative-serial fix moved 393 cells from `d/d` into the correct numeric/date adjudication without changing the underlying disagreement. The sealed counts are therefore 112,091 `d/d` millisecond cases, 78,005 `n/d` duration cases, and 393 additional `n/d` negative-date cases—not the pre-fix 112,484 and 78,005 totals stated above. This appended row corrects the historical snapshot without rewriting the append-only entries. | 2026-07-28 |
| `corpus/files/{sheetjs/test_files/number_format.xlsm,sheetjs/test_files/LONumbers.xlsx,sheetjs/test_files/LONumbers-2010.xlsx,sheetjs/test_files/LONumbers-2011.xlsx,poi/test-data/spreadsheet/bug60858.xlsx}` | negative serials under date-like formats (representative: `LONumbers.xlsx`, sheet 1 `C3`) | `t` / `v` | wax correct; SheetJS fabricates pre-epoch dates | `C3` stores numeric `-1.2` under `YY`. Excel's 1900 date system cannot display a negative serial as a date; wax retains `t:"n", v:-1.2` and no display. SheetJS changes it to `t:"d", v:"1899-12-29T19:12:00"` while its own display is empty. The same evidence covers all 393 negative-serial `n/d` cases. | 2026-07-28 |
| `corpus/files/{poi/test-data/spreadsheet/47251.xls,sheetjs/test_files/apachepoi_47251.xls,sheetjs/test_files/apachepoi_47251.xls.xlsb}` | locale-qualified formats (representative: sheet 1 `F13`) | `d` | wax correct; SheetJS mistakes a locale identifier for a currency token | `F13` stores `0.0042` with `[$-1010409]0.000%`. The bracket is a locale identifier and contains no currency symbol, so wax renders `0.420%`. SheetJS renders `$0.420%`, inventing a dollar sign. This exact error accounts for all 132 `[$-1010409]0.000%` display mismatches. | 2026-07-28 |
